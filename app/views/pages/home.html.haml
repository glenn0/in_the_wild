- title("RSpec in the Wild")
.jumbotron
  %h1.center
    RSpec in the Wild
  %p Share real world RSpec examples. Learn from the pros!
  = bootstrap_form_for @project, style: :inline do |f|
    = f.text_field :url, hide_label: true, placeholder: "eg. https://github.com/rspec/rspec"
    = f.submit "Add GitHub Repo!"

-#
    %fieldset
      = f.text_field :url, hide_label: true, placeholder: "eg. https://github.com/rspec/rspec"
    %fieldset.actions.control-group
      %input(type="submit" value="Submit Project" class="btn")

    .input-group
      %input.form-control{:type => "text"}
        %span.input-group-btn
          %button.btn.btn-default{:type => "submit"} Add GitHub Repo!
%br

- @all_projects.reverse.each do |p|
  .well 
    .row
      .col-md-10
        .panel.panel-default
          .panel-heading
            %h2.panel-title
              = link_to p.repo_name, p.repo_url
          .panel-body
            = p.repo_description
            %br
            %br
            - if p.tags.count > 0
              It tests with: 
              - p.tags.each do |t|
                %span.label.label-default= link_to t.name, "#{p.repo_url}/tree/master/spec/#{t.name}"
            .pull-right
              Submitted by
              - p.users.each do |u|
                = u.username
              = time_ago_in_words(p.created_at) + " ago"
      .col-md-2
        %a.pull-right{:href => "#{p.repo_owner_url}"}
          =image_tag "https://gravatar.com/avatar/#{p.repo_owner_avatar}?s=120"
          .pull-right
            = link_to p.repo_owner, p.repo_owner_url